<ul class="nav-items">
    <% if (!locals.isAuth) { %>
        <li><a href="/">Shop</a></li>
        <li>
            <a href="/cart">Cart
                <span class="badge">
                    <%= locals.cart.totalQuantity %>
                </span>
            </a>
        </li>
        <li><a href="/signUp">SignUp</a></li>
        <li><a href="/login">Login</a></li>
        <% } %>

            <% if (locals.isAuth&&!locals.isAdmin) { %>
                <li><a href="/orders">Orders</a></li>
                <li><a href="/">Shop</a></li>
                <li>
                    <a href="/cart">Cart
                        <span class="badge">
                            <%= locals.cart.totalQuantity %>
                        </span>
                    </a>
                </li>
         
                <li>
                    <% } %>
                        <% if(locals.isAdmin) {%>
                <li><a href="/main/products">Manage Products</a></li>
                <li><a href="/main/orders">Manage Orders</a></li>
                <% } %>
                    <% if (locals.isAuth) { %>

                        <form action="/logout" method="POST">
                            <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
                            <button>logout</button>
                        </form>

                        </li>
                        <% } %>
</ul>